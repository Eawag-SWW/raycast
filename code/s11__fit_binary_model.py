"""
Args:
 - retrain binary model with latest image samples
Writes:
 - settings for binary model
"""


def retrain_binary_model(config, debug):
    # 'binary_model_path': os.path.join(previous_iteration_dir, settings.general['iterations_structure'][6],
    #                                   'binary_model.py'),
    return 0
